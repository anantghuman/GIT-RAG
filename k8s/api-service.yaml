apiVersion: v1
kind: Service
metadata:
  name: git-rag-api
  namespace: git-rag
spec:
  type: ClusterIP
  selector:
    app: git-rag-api
  ports:
    - name: http
      port: 80
      targetPort: 8000
